# Secrets — never commit
.env
.env.local
.env.*.local

# Node / Next.js
node_modules/
.next/
out/
build/
dist/

# System
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Vercel
.vercel

# Next.js typegen
next-env.d.ts
tsconfig.tsbuildinfo

# Obsidian workspace state (keep the vault content, ignore Obsidian's own UI state)
.obsidian/workspace*
.obsidian/appearance.json

# Word/Office lock & temp files
~$*

# Other AfriShield clients' projects live in subfolders of this repo but are
# separate codebases with their own git history. The blog-publishing routine
# runs `git add -A`, which would otherwise sweep them (and their node_modules)
# into NJ's repo. Per CLAUDE.md, another client's data does not belong here.
Afrishield Clients/
Reciept tracker/
AI SEO website builder/
